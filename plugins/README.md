# Plugins

Everything beyond the core is a plugin. Plugins live in `plugins/<Name>/` and are opt-in.

See [docs/plugin-contract.md](../docs/plugin-contract.md) for the spec on building and installing plugins.
